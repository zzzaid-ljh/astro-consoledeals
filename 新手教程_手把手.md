# 游戏折扣站 · 小白手把手教程（从空白到上线拿佣金）

> 你是兼职试水，本教程默认你**零基础**。每一步都给了「去哪个网页、点什么、填什么」。
> 我（AI）没法替你申请联盟账号——因为要绑定**你本人的邮箱、收款方式和税务信息**。所以申请这步必须你自己点，但我会把每一步都指到位置。

---

## 〇、你现在手里有什么 / 还差什么

✅ **已经有了（我帮你搭好的）**
- 网站代码：桌面 `GameDealsSite/` 文件夹
- 依赖已装好（`node_modules`，122 MB 在 C 盘，可接受）
- 能本地预览、能构建（`npm run build` 已验证通过）

❌ **还差这几步（本教程覆盖）**
1. 申请联盟账号 → 拿到各家追踪 ID
2. 把 ID 填进 `src/config.ts`
3. 本地预览看实物（`npm run dev`）
4. 部署上线（免费托管，拿到真实网址）
5. 日常加 deal / 写文章

---

## 一、申请联盟账号（拿追踪 ID）

### 推荐顺序与理由
`Humble → GMG → Fanatical → 亚马逊 → AdSense`

前三家（数字游戏联盟）**没流量门槛、30 天 cookie、佣金 5–10%**，新手站刚建就能申，最容易过。
亚马逊有「180 天 3 单否则封号」的死线，建议等站有点流量再申。AdSense 放最后。

### 💡 关键技巧：先拿域名再去申请，通过率更高
申请联盟时都要填「你的网站地址」。如果你拿「还没上线的站」去填，容易被拒。
**所以实操建议：先跳到第 四 步把站部署到 Cloudflare Pages（免费、10 分钟），拿到 `xxx.pages.dev` 域名，再用这个域名去申请联盟。** 本教程按这个更顺的顺序写。

---

### 1.1 申请前先备好（一次性）
- 一个常用邮箱（推荐 Gmail）
- 一个 **PayPal** 账号（绝大多数联盟用 PayPal 打款，没有就去 paypal.com 注册）
- 你的站点域名（见第四步部署后获得，形如 `gamedeals-xxxx.pages.dev`）

---

### 1.2 Humble Bundle（经 Impact 平台）—— 推荐第一个申
1. 打开 **https://app.impact.com/** → 注册一个 Impact 账号
2. 登录后点顶部 **Brands / Discover**，搜索 **"Humble Bundle"**
3. 点 **Join Program / Apply**，填申请表：
   - **Website**：填你的站点域名
   - **Traffic & Promotions**：简单描述你怎么做内容，例如填 `Game deals blog with SEO buying-guide articles`
   - **Payment**：绑定你的 PayPal
4. 提交后一般 **1–2 天**审核，也有自动秒过
5. 通过后，在后台找到你的 **Tracking ID / Partner ID**（一串字符）
6. 把它填进 `config.ts` 的 `HUMBLE_PARTNER`

---

### 1.3 Green Man Gaming（经 Tapfiliate）
1. 搜索 **"Green Man Gaming affiliate program"** 或去 GMG 官网底部找 **Affiliates** 入口
2. 注册后，后台会给你 **两个值：`tap_a` 和 `tap_s`**
3. 分别填进 `config.ts` 的 `GMG_TAP_A` 和 `GMG_TAP_S`

---

### 1.4 Fanatical（经 Partnerize 或自有后台）
1. 搜索 **"Fanatical affiliate program"** 进入申请页
2. 通过后拿到 **affiliate ID**
3. 填进 `config.ts` 的 `FANATICAL_AFF`

---

### 1.5 Amazon Associates（有封号死线，谨慎）
1. 打开 **https://affiliate-program.amazon.com/** → 用你的亚马逊账号登录
2. 填写网站 / App 信息，提交后拿到 **Tracking ID**，形如 `yourname-20`
3. 填进 `config.ts` 的 `AMAZON_TAG`
4. ⚠️ **两条铁律（踩了就白干）：**
   - **180 天内必须产生 3 笔有效订单**，否则账号关闭 → 别第一天就注册，等站有点流量再申，或申完立刻认真推
   - **24 小时 cookie**：访客点了你链接后 24 小时内没下单，佣金不算
   - 不要手标价格、不要用短链（bit.ly）、邮件里只能链你的文章页、不能直接放联盟链接

---

### 1.6 Google AdSense（广告变现，最后做）
1. 打开 **https://www.google.com/adsense/** → 用 Gmail 登录
2. 添加你的站点，按要求填税务信息（个人选 Individual）
3. 需要：**站点已上线 + 有隐私政策页**（我们的 `/privacy/` 已备好，满足）
4. 通过后拿到 **发布商 ID（ca-pub-xxxx）** 和一段 `<script>` 广告代码
5. ⚠️ 当前网站模板**还没接 AdSense 广告位代码**。等你 AdSense 通过，回来告诉我，我帮你把广告位加进页面（这一步需要改代码，不是填 config 就行）

---

## 二、把 ID 填进 config.ts（只需改 4 行 + 4 个开关）

1. 用 **记事本** 或 **VS Code** 打开：
   `C:\Users\LJH\Desktop\GameDealsSite\src\config.ts`
2. 滚到文件**最顶部**，你会看到 4 组大写常量，把占位符换成你申请到的值：

   ```ts
   const AMAZON_TAG      = "yourname-20";          // ← 亚马逊 Tracking ID
   const GMG_TAP_A       = "abc123";               // ← GMG 后台 tap_a
   const GMG_TAP_S       = "xyz789";               // ← GMG 后台 tap_s
   const HUMBLE_PARTNER  = "你的partnerID";         // ← Humble/Impact Tracking ID
   const FANATICAL_AFF   = "你的affID";             // ← Fanatical affiliate ID
   ```

3. 往下一点，每个网络有 `enabled: false`，**等你真的开通了那家，就把对应的改成 `enabled: true`**（不是所有都改，开通哪家改哪家）。
4. 保存文件即可。

✅ **验证是否填对**：运行 `npm run dev`（见第三步），打开首页点任意一个绿色 **「View Deal」** 按钮，看浏览器地址栏的链接里有没有带上你的 ID 参数（比如 `?partner=你的ID` 或 `?tag=yourname-20`）。有 = 成功。

---

## 三、本地预览（看实物）

1. 打开「终端」：
   - 如果你装了 **VS Code**：在 `GameDealsSite` 文件夹里右键 →「通过 Code 打开」→ 菜单「终端」→「新建终端」
   - 或者直接用系统「命令提示符」
2. 逐行输入（每输完一行按回车）：
   ```
   cd C:\Users\LJH\Desktop\GameDealsSite
   npm run dev
   ```
3. 看到终端打印出 `http://localhost:4321/` 就成功了
4. 浏览器打开 **http://localhost:4321** → 看到首页一排游戏 deal 卡片 = 一切正常
5. 想关掉预览：在终端按 `Ctrl + C`

> 注：我刚才已经帮你启动了一个预览服务，你现在直接开 **http://localhost:4321** 就能看。但这次会话结束后它就停了，以后要预览就自己跑上面两步。

---

## 四、部署上线（免费托管，拿到真实网址）

### 方案 A：Cloudflare Pages（✅ 推荐，免费、自动更新、无需信用卡）

**第 1 部分：把代码推到 GitHub（一次性）**
1. 注册 **https://github.com/**（免费）
2. 新建仓库：右上角 **＋** → **New repository** → 名字填 `game-deals` → 选 **Public** → 点 Create
3. 在 `GameDealsSite` 文件夹里打开 Git Bash（右键 → Git Bash Here），逐行输入：
   ```
   git init
   git add .
   git commit -m "first commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/game-deals.git
   git push -u origin main
   ```
   （把 `你的用户名` 换成你 GitHub 的账号名）

**第 2 部分：连 Cloudflare Pages**
1. 打开 **https://pages.cloudflare.com/** → 用邮箱注册 / 登录
2. 点 **Create a project** → 选 **Connect to Git** → 授权连 GitHub → 选 `game-deals` 仓库
3. 构建设置填这两项（重点，别填错）：
   - **Build command**：`npm run build`
   - **Build output directory**：`dist`
4. 点 **Save and Deploy**，等 1–2 分钟 → 拿到网址，形如 `https://game-deals-xxx.pages.dev`
5. ✅ 以后每次改完内容，只要 `git push` 一下，Cloudflare 会自动重新部署，不用手动操作

**部署前必做**：打开 `astro.config.mjs`，把 `site: "https://your-domain-here.com"` 改成你的真实域名（影响 sitemap 和绝对链接）。

### 方案 B：Netlify Drop（最简单，但每次要手动重传）
1. 打开 **https://app.netlify.com/drop**
2. 把项目里的 **`dist` 文件夹**直接拖进网页
3. 立刻拿到域名
缺点：你每次改内容都要重新 `npm run build` 再拖一次，不如方案 A 省事。新手图省事可用，长期推荐 A。

---

## 五、日常更新（加新 deal / 写文章）

- **加每周 deal**：用记事本打开 `src/data/deals.json`，照着里面已有的示例，复制一条改掉游戏名、价格、链接即可。
- **写新文章**：进 `src/pages/games/` 文件夹，复制 `mario-kart-8-deluxe.astro` 这个文件，改名、改里面的文字和链接。
- **改完上线**：本地 `npm run dev` 先看效果 → 满意了 `npm run build` → `git push` 自动部署。

---

## 六、避坑清单（照着做就不会被封 / 不下架）

- ✅ 每个含联盟链接的页面**底部都有 FTC 声明**（模板 Footer 已内置，别删）
- ✅ 价格用「Check on Amazon / View Deal」按钮跳转，**绝不手写在页面上**（价格会变，手写违反 Amazon 规则）
- ✅ 联盟链接**不加短链**（bit.ly 等），用模板自动生成的原始链接
- ✅ 邮件营销里**只能链你的文章页**，不能直接放联盟链接
- ✅ 亚马逊 **180 天 3 单**死线记牢；**24 小时 cookie** 记牢
- ✅ 广告（AdSense）通过前，页面先别放广告代码（等我去加）
- ✅ 前 6 个月大概率收入很少（$0–200/月），这是试水，别影响主业

---

## 七、时间线参考（现实预期）

| 阶段 | 时间 | 你在做什么 |
|------|------|-----------|
| 搭建期 | 第 1 周 | 部署上线 + 填好联盟 ID + 写 5–10 篇内容 |
| 申请期 | 第 1–2 周 | 申 Humble/GMG/Fanatical（秒过/1–2天）→ 申亚马逊 → 挂 AdSense |
| 内容期 | 第 1–3 月 | 扩到 30–50 篇，长尾 SEO 起量 |
| 变长期 | 第 3–6 月 | 月访问破万，广告收入开始有意义 |

**记住：这是副业现金流试水，前半年别指望赚钱，主业优先。**

---

## 八、需要我继续帮你的下一步（任选）

1. 帮你把示例 `deals.json` 换成你**真正想推**的游戏清单
2. 你 AdSense 通过后，帮你把**广告位代码**接进页面
3. 帮你批量生成第一批 **10–15 篇攻略文章骨架**（用写作引擎）
4. 帮你部署到 Cloudflare Pages 并出在线预览链接（如果你不想自己推 GitHub）

直接告诉我要做哪个。
